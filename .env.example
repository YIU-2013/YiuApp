# ─── App Environment (örnek) ──────────────────────────────────────────────────
# Gerçek değerlerle .env dosyanızı oluşturmak için:
#   cp .env.example .env
# .env / .env.production asla commitlenmemeli (bkz. .gitignore).

APP_ENV=development
API_BASE_URL=https://dev-api.example.edu.tr/v1
SENTRY_DSN=
